# altaria

## Prerequisites

- [tmux](https://github.com/tmux/tmux) — required for terminal session management

  ```bash
  # macOS
  brew install tmux

  # Debian / Ubuntu
  sudo apt install tmux

  # Fedora
  sudo dnf install tmux
  ```
